language: node_js
node_js:
  - stable

cache:
  directories:
    - "node_modules"

before_install:
  - npm install -g yarn
  - export PATH="$HOME/.yarn/bin:$PATH"

before_script:
  - yarn install
script:
  - yarn run unit-test
